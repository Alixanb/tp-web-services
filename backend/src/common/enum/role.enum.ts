export enum UserRole {
  CLIENT = 'client',
  ORGANIZER = 'organizer',
  ADMIN = 'admin',
}

export enum OrderStatus {
  PENDING = 'pending',
  PAID = 'paid',
  CANCELLED = 'cancelled',
  REFUNDED = 'refunded',
}
